---
- name: remove IPv6 address for localhost
  - lineinfile:
      path: /etc/hosts
      state: absent
      regexp: '^::1'

- name: add IPv4 address for localhost
  - lineinfile:
      path: /etc/hosts
      regexp: '^127.0.0.1\s'
      line: '127.0.0.1 localhost'
