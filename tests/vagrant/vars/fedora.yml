glusterfs_server_package_info:
  pre_pkgs:
    - libcmocka-devel
    - cifs-utils
    - hostname
    - man-db
    - nfs-ganesha-gluster
    - procps-ng
    - samba*
  del_pkgs:
    - glusterfs*

glusterfs_server_services:
  disabled:
    - nfs-server
